## FULL STACK DEVELOPMENT PROJECT

---

`Username : test`
`Password : testing`

**DB STRUCTURE**
`MYSQL DB Script in api > cegis.sql`

**RUN the Project**

- [ ] Create Database
- [ ] Import the DB script from api folder named cegis.sql
- [ ] Go to api folder. Edit your config file. Add database connection in config file
- [ ] Go to root folder. Edit config.js -> Add the api URL (Add a forward slash at end)
- [ ] HOST the project
